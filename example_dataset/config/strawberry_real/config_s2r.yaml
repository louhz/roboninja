# Dataset root that contains episodes_1, episodes_2, ...
parent: /home/louhz/Desktop/Rss/roboninja/example_dataset/sim2real

# Same options as CLI (global defaults)
normalize: center           # none | center | bbox
axis_order: [0, 1, 2]
axis_signs: [1, 1, 1]
mesh_subdir: mesh
backup: true
dry_run: false
report_csv: /home/louhz/Desktop/Rss/roboninja/sim2real/mesh_report.csv

# Per-episode lists (1-based episode id -> 0-based list index) # based on foundationpose and point cloud observation
scales: 
  - [0.5, 0.5, 0.5]  # IT IS ACTUALLY 0.05 TO 0.1 I ALREADY DID IT MANUALLY

  
offsets:
  - [0.49, -0.46, 0.41]     # for episodes_1
